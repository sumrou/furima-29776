const pay = ()=> {
  Payjp.setPublickey("公開鍵"); //テスト公開鍵
  const form = document.getElementById("charge-form");
  form.addEventListener("submit", (e) => {
    e.preventDefault();

    const formResult = document.getElementById()
  });
}

window.addEventListener("load", pay);
